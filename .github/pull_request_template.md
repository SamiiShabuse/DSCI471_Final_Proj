# Summary

Describe what changed and why.

## Validation

- [ ] Ran `python src/prepare_data.py --check`, if data preparation changed
- [ ] Ran `python src/evaluate.py --baseline-only`, if evaluation changed
- [ ] Ran `python scripts/run_sample_pipeline.py`, if pipeline behavior changed
- [ ] Checked documentation links, if docs changed

## Data and Artifact Check

- [ ] No Kaggle raw data is included
- [ ] No credentials, `.env` files, or local absolute paths are included
- [ ] Model weights or generated artifacts are intentional and documented

## Notes

Add metric changes, query style details, or reproduction notes.